# Enable browser caching using mod_expires
<IfModule mod_expires.c>
    ExpiresActive On
    
    # CSS y JS deben revalidarse siempre (0 segundos) para permitir actualizaciones
    ExpiresByType application/javascript "access plus 0 seconds"
    ExpiresByType text/javascript "access plus 0 seconds"
    ExpiresByType text/css "access plus 0 seconds"
    ExpiresByType text/html "access plus 1 hour"
    
    # Cache de Imágenes y Fuentes a largo plazo
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    ExpiresByType font/woff "access plus 1 year"
    ExpiresByType font/woff2 "access plus 1 year"
    ExpiresByType application/vnd.ms-fontobject "access plus 1 year"
    ExpiresByType application/x-font-ttf "access plus 1 year"
</IfModule>

# Set public cache control header for static files using mod_headers
<IfModule mod_headers.c>
    # Imágenes y fuentes inmutables
    <FilesMatch "\.(gif|png|jpg|jpeg|svg|woff|woff2|eot|ttf)$">
        Header set Cache-Control "public, max-age=31536000, immutable"
    </FilesMatch>
    
    # JS y CSS sin caché del navegador (el Service Worker gestionará su caché de forma óptima)
    <FilesMatch "\.(js|css)$">
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "-1"
    </FilesMatch>
</IfModule>

# Prevent caching for the Service Worker itself so updates are detected immediately
<Files "firebase-messaging-sw.js">
    <IfModule mod_expires.c>
        ExpiresActive Off
    </IfModule>
    <IfModule mod_headers.c>
        Header set Cache-Control "no-cache, no-store, must-revalidate"
        Header set Pragma "no-cache"
        Header set Expires "-1"
    </IfModule>
</Files>
